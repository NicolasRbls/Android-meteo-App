package com.example.android_meteo_app.ui.theme

import androidx.compose.ui.graphics.Color

val PrimaryBlue = Color(0xFF2196F3) // Bleu primaire
val LightBlue = Color(0xFF64B5F6)   // Bleu clair
val DarkBlue = Color(0xFF1976D2)    // Bleu foncé
val AccentYellow = Color(0xFFFFC107) // Jaune pour le soleil/accent
val TextWhite = Color(0xFFFFFFFF)   // Texte blanc
val TextBlack = Color(0xFF212121)   // Texte noir
val BackgroundLight = Color(0xFFE3F2FD) // Arrière-plan clair
val BackgroundDark = Color(0xFF121212)  // Arrière-plan sombre (pour le mode nuit)
val SurfaceLight = Color(0xFFBBDEFB) // Surface claire
val SurfaceDark = Color(0xFF424242)  // Surface sombre

// Gradient colors for weather conditions
val SunnyGradientStart = Color(0xFFFBC02D) // Amber 700 (darker golden yellow)
val SunnyGradientEnd = Color(0xFFFDD835)   // Amber 600 (used as end color for warmth)

val CloudyGradientStart = Color(0xFFB0BEC5) // Blue Grey 300
val CloudyGradientEnd = Color(0xFF78909C)   // Blue Grey 500

val RainyGradientStart = Color(0xFF607D8B)  // Blue Grey 500
val RainyGradientEnd = Color(0xFF455A64)    // Blue Grey 700

val UnknownGradientStart = Color(0xFF9E9E9E) // Grey 500
val UnknownGradientEnd = Color(0xFF616161)   // Grey 700